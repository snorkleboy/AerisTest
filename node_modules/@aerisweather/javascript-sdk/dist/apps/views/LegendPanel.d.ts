import Panel, { PanelOptions, PanelState } from '../ui/Panel';
import LegendView from '../../maps/views/legends/LegendView';
import { LegendOptions } from '../../maps/views/legends/types';
import { ISize } from '../../interfaces/ICoordinate';
/**
 * A data type that represents the configuration options for a legend panel.
 */
export declare type LegendPanelOptions = PanelOptions & {
    /**
     * Configuration options for the legend view.
     *
     * @type {LegendOptions}
     */
    legend: LegendOptions;
};
declare type LegendPanelState = PanelState & {};
/**
 * A `LegendPanel` is a panel component that renders and manages a `LegendView` instance.
 *
 * @export
 * @class LegendPanel
 * @extends {Panel<LegendPanelOptions, LegendPanelState>}
 */
export default class LegendPanel extends Panel<LegendPanelOptions, LegendPanelState> {
    private _legendView;
    /**
     * The underlying legend view instance.
     *
     * @readonly
     * @type {LegendView}
     * @memberof LegendPanel
     */
    readonly legend: LegendView;
    /**
     * Initializes and returns a legend panel instance configured with the specified options.
     *
     * @param {LegendPanelOptions} [opts]
     * @memberof LegendPanel
     */
    constructor(opts?: LegendPanelOptions);
    open(): void;
    contentSize(): ISize;
    protected _onAddedToDOM(): void;
    protected _setupEvents(): void;
}
export {};
