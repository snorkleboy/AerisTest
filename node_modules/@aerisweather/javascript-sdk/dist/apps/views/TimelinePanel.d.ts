import Panel, { PanelOptions, PanelState } from '../ui/Panel';
import Animation from '../../maps/anim/Animation';
import { RangeSliderOptions } from '../ui/RangeSlider';
/**
 * A data type that represents the configuration options for a timeline panel.
 */
export declare type TimelinePanelOptions = PanelOptions & {
    /**
     * Configuration options for the range slider component.
     *
     * @type {RangeSliderOptions}
     */
    range: RangeSliderOptions;
};
export declare type TimelinePanelState = PanelState & {};
/**
 * A `TimelinePanel` is a panel component that renders and manages a timeline animation slider
 * control that allows a user to scrub an animation's current time position and change the
 * animation start/end time range.
 *
 * @export
 * @class TimelinePanel
 * @extends {Panel<TimelinePanelOptions, TimelinePanelState>}
 */
export default class TimelinePanel extends Panel<TimelinePanelOptions, TimelinePanelState> {
    private _timeline;
    private _indicator;
    private _animation;
    private _tooltip;
    private _slider;
    /**
     * Initializes and returns a timeline panel instance configured with the specified animation and options.
     * @param {Animation} animation
     * @param {TimelinePanelOptions} [opts]
     * @memberof TimelinePanel
     */
    constructor(animation: Animation, opts?: TimelinePanelOptions);
    protected _render(): string;
    protected _onAddedToDOM(): void;
    protected _setupEvents(): void;
    private _setTime;
}
