import PanelView, { PanelOptions, PanelState } from '../ui/Panel';
import SearchField, { SearchFieldOptions } from '../ui/SearchField';
import Account from '../../account/Account';
/**
 * A data type that represents the configuration options for a place search panel.
 */
export declare type PlaceSearchPanelOptions = PanelOptions & {
    /**
     * Aeris account to use when performing place search requests.
     *
     * @type {Account}
     */
    account: Account;
    /**
     * Configuration options for the search field component.
     *
     * @type {SearchFieldOptions}
     */
    field?: SearchFieldOptions;
};
declare type PlaceSearchPanelState = PanelState & {};
/**
 * A `PlaceSearchPanel` is a panel component that displays and manages a search field component
 * that can be used for searching for and selecting locations.
 *
 * @export
 * @class PlaceSearchPanel
 * @extends {PanelView<PlaceSearchPanelOptions, PlaceSearchPanelState>}
 */
export default class PlaceSearchPanel extends PanelView<PlaceSearchPanelOptions, PlaceSearchPanelState> {
    private _account;
    private _service;
    private _field;
    /**
     * The underlying search field component.
     *
     * @readonly
     * @type {SearchField}
     * @memberof PlaceSearchPanel
     */
    readonly field: SearchField;
    /**
     * Initializes and returns a place search panel instance configured with the specified options.
     *
     * @param {PlaceSearchPanelOptions} [opts]
     * @memberof PlaceSearchPanel
     */
    constructor(opts?: PlaceSearchPanelOptions);
    protected _onAddedToDOM(): void;
    protected _setupEvents(): void;
    private _prepareServiceForQuery;
}
export {};
