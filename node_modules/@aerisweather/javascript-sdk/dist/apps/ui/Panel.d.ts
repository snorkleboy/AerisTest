import Component, { ComponentOptions, ComponentState } from "./Component";
import { ISize } from '../../interfaces/ICoordinate';
/**
 * A data type that represents the configuration options for a panel component.
 */
export declare type PanelOptions = ComponentOptions & {
    /**
     * Panel title, which is displayed when a toggleable panel is expanded.
     *
     * @type {string}
     */
    title?: string;
    /**
     * A Boolean indicating whether the panel is toggleable. Default value is `false`.
     *
     * @type {boolean}
     */
    toggleable?: boolean;
    /**
     * Icon HTML to display in the button when the panel is collapsed. This value can be any valid
     * HTML, including an `<img>` or `<svg>` element.
     *
     * @type {string}
     */
    icon?: string;
};
/**
 * A data type that represents a panel's state.
 */
export declare type PanelState = ComponentState & {
    /**
     * A Boolean indicating whether the panel is currently visible.
     *
     * @type {boolean}
     */
    visible: boolean;
};
/**
 * A `Panel` is a view component that functions as a common view container for any type of
 * content within an application.
 *
 * @export
 * @class Panel
 * @extends {Component<T, S>}
 * @template T
 * @template S
 */
export default class Panel<T extends PanelOptions, S extends PanelState> extends Component<T, S> {
    private _bounds;
    /**
     * Initializes and returns a panel instance configured with the specified options.
     *
     * @param {T} [opts]
     * @memberof Panel
     */
    constructor(opts?: T);
    /**
     * A Boolean indicating whether the panel is currently visible.
     *
     * @readonly
     * @type {boolean}
     * @memberof Panel
     */
    readonly visible: boolean;
    /**
     * Expands the panel if the panel is toggleable and collapsed.
     *
     * @returns
     * @memberof Panel
     */
    open(): void;
    /**
     * Collapses the panel if the panel is toggleable and expanded.
     *
     * @returns
     * @memberof Panel
     */
    close(): void;
    /**
     * Toggles the open/closed state.
     *
     * @memberof Panel
     */
    toggle(): void;
    /**
     * Returns the DOM element container for the view content.
     *
     * @returns {*}
     * @memberof Panel
     */
    getContentContainer(): any;
    /**
     * Returns the size required for the panel's content.
     *
     * @returns {ISize}
     * @memberof Panel
     */
    contentSize(): ISize;
    protected _updateSize(): void;
    protected _render(): string;
    protected _layout(): void;
    protected _afterRender(): void;
    protected _onAddedToDOM(): void;
    protected _setupEvents(): void;
}
