import Component, { ComponentOptions, ComponentState } from './Component';
/**
 * A data type that represents the configuration options for a slider handle.
 */
declare type HandleOptions = ComponentOptions & {
    values: {
        min: number;
        max: number;
        current: number;
    };
    disabled?: boolean;
    tabindex?: number;
};
declare type HandleState = ComponentState & {
    clickFocused: boolean;
};
export default class SliderHandle extends Component<HandleOptions, HandleState> {
    constructor(opts: HandleOptions);
    setClickFocus(focused: boolean): void;
    clickFocus(): void;
    focus(): void;
    blur(): void;
    update(offset: number, opts?: HandleOptions): void;
    protected _afterRender(): void;
    private _handleMouseDown;
    private _handleMouseUp;
    private _handleBlur;
}
export {};
