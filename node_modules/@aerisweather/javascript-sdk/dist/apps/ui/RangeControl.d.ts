import View, { ViewOptions } from './View';
export declare type RangeItem = {
    value: number;
    label: string;
};
export declare type NumericRange = {
    min: number;
    max: number;
};
export declare type RangeControlOptions = ViewOptions & {
    title?: string;
    intervals?: RangeItem[] | number[] | string;
    range?: {
        min: number;
        max: number;
        interval: number;
    };
};
export default class RangeControl extends View<RangeControlOptions> {
    private _range;
    private _intervals;
    readonly intervals: RangeItem[];
    constructor(opts?: RangeControlOptions);
    setIntervals(intervals: RangeItem[] | number[]): void;
    setRange(min: number, max: number, interval: number): void;
    value(range?: NumericRange): any;
    protected _render(): string;
    protected _setupEvents(): void;
    private _handleClick;
    private _select;
}
