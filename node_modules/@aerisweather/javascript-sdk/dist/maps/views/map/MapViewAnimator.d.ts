import Animation from '../../anim/Animation';
import { ImageAnimationProvider } from '../../anim/ImageAnimation';
import Timeline from '../../anim/Timeline';
declare type AnimationTargets = {
    past: HTMLElement;
    future: HTMLElement;
    pastText: HTMLElement;
    futureText: HTMLElement;
};
export declare type MapViewAnimatorOptions = {
    enabled: boolean;
    autoplay: boolean;
    alwaysShowPast: boolean;
    alwaysShowFuture: boolean;
};
export default class MapViewAnimator {
    private _targets;
    private _opts;
    private _timeline;
    readonly timeline: Timeline;
    provider: ImageAnimationProvider;
    readonly past: Animation;
    readonly future: Animation;
    readonly pastText: Animation;
    readonly futureText: Animation;
    constructor(targets: AnimationTargets, opts: MapViewAnimatorOptions);
    private init;
}
export {};
