/// <reference types="mapbox-gl" />
import MapboxLayer from './Layer';
import { PolygonDef } from '../../../../interfaces/IOverlayFactory';
export default class MapboxPolygonLayer extends MapboxLayer {
    private _polygons;
    private _opts;
    private _strokeLayer;
    constructor(layer: string, polygons: PolygonDef[], opts?: any);
    readonly strokeLayerId: string;
    addTo(map: mapboxgl.Map): void;
    remove(): void;
    invalidate(): void;
    _style(key: string): any;
}
