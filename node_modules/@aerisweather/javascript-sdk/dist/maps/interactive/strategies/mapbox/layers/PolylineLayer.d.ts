import MapboxLayer from './Layer';
import { ICoordinate } from '../../../../../interfaces/ICoordinate';
export default class MapboxPolylineLayer extends MapboxLayer {
    private _points;
    private _opts;
    constructor(layer: string, points: ICoordinate[], opts?: any);
    invalidate(): void;
    _style(key: string): any;
}
