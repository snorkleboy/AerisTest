import OpenLayersLayer from './Layer';
import { PolygonDef } from '../../../../interfaces/IOverlayFactory';
export default class OpenLayersPolygonLayer extends OpenLayersLayer {
    data: any;
    private _polygons;
    private _opts;
    constructor(layer: string, polygons: PolygonDef[], opts?: any);
    addTo(map: ol.Map, index?: number): void;
    invalidate(): void;
}
