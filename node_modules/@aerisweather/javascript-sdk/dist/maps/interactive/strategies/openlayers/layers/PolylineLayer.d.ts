import OpenLayersLayer from './Layer';
import { ICoordinate } from '../../../../../interfaces/ICoordinate';
export default class OpenLayersPolylineLayer extends OpenLayersLayer {
    data: any;
    private _points;
    private _opts;
    constructor(layer: string, points: ICoordinate[], opts?: any);
    addTo(map: ol.Map, index?: number): void;
    invalidate(): void;
}
