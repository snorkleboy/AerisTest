import { ICoordinate } from '../../../../interfaces/ICoordinate';
export interface PopupOptions {
    closeButton?: boolean;
    closeOnClick?: boolean;
    anchor?: string;
    offset?: string;
    className?: string;
}
export default class Popup {
    private _overlay;
    private _map;
    private _container;
    constructor(coordinate?: ICoordinate);
    addTo(map: ol.Map): void;
    remove(): void;
    setCoordinate(coord: ICoordinate): void;
    setHTML(html: string): void;
    setDOMContent(node: HTMLElement): void;
    static injectStyles(): void;
}
