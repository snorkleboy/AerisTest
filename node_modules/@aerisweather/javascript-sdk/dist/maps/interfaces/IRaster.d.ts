import RasterStyle from '../interactive/style/RasterStyle';
/**
 * An interface that represents a map shape configuration, such as a polygon or polyline. This
 * provides a common interface for configuring map shapes regardless of which mapping strategy is
 * used.
 */
export default interface IRaster {
    /**
     * Style options for the shape.
     */
    style?: RasterStyle;
}
