import IMapStrategy from './IMapStrategy';
import Animation from '../anim/Animation';
export default interface IMapContentSource {
    /**
     * Identifier associated with the data source.
     *
     * @type {string}
     * @memberof IMapContentSource
     */
    key: string;
    /**
     * Map strategy the source is associated with.
     *
     * @type {IMapStrategy}
     * @memberof IMapContentSource
     */
    strategy?: IMapStrategy;
    /**
     * Whether the data source is enabled.
     *
     * @type {boolean}
     * @memberof IMapContentSource
     */
    enabled: boolean;
    animation(): Animation;
}
