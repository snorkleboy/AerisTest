import Geometry from './Geometry';
import { ICoordinate, LatLonValue } from '../../interfaces/ICoordinate';
import CoordinateBounds from '../../geo/CoordinateBounds';
declare class PolygonGeometry {
    coordinates: ICoordinate[];
    constructor(vertices: LatLonValue[]);
}
declare class Polygon implements Geometry {
    coordinates: ICoordinate[][];
    outer: PolygonGeometry;
    inner: PolygonGeometry[];
    constructor(coords: LatLonValue[][]);
    toArray(reversed?: boolean): any[];
    bounds(): CoordinateBounds;
}
export { PolygonGeometry };
export default Polygon;
