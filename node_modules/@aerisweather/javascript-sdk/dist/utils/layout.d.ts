import { ISize, IPoint } from '../interfaces/ICoordinate';
/**
 * A data type that represents the supported anchor positions for elements.
 */
export declare type AnchorType = 'topleft' | 'top' | 'topright' | 'left' | 'center' | 'right' | 'bottomleft' | 'bottom' | 'bottomleft';
export declare const getPinnedPosition: (position: "bottom" | "left" | "right" | "top" | "center" | IPoint | "topleft" | "topright" | "bottomleft", size: ISize, container: ISize, translate?: IPoint) => IPoint;
export declare type ViewportSize = 'normal' | 'compact' | 'desktop';
export declare const viewportSizeClass: () => {
    width: ViewportSize;
    height: ViewportSize;
};
